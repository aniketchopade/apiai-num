{
  "name": "Add",
  "description": "Add two numbers",
  "version": "0.0.1",
  "private": true,
  "license": "Apache Version 2.0",
  "author": "Aniket",
  "engines": {
    "node": "~4.2"
  },
  "scripts": {
    "lint": "semistandard --fix \"**/*.js\"",
    "start": "functions deploy carBooker --trigger-http",
    "deploy": "gcloud beta functions deploy carBooker --trigger-http --stage-bucket aniket-chopade-bucket"
  },
  "dependencies": {
    "actions-on-google": "^1.0.0",
    "request-promise": "4.2.0",
     "request": "^2.34",
     "ssml-builder":  "0.1.0"    
  },
  "devDependencies": {
    "semistandard": "^9.1.0"
  }
}
